import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-pd-app',
  webDir: '../../dist/apps/ionic-pd-app',
  bundledWebRuntime: false,
};

export default config;
